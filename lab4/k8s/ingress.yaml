apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: ingress-controller
spec:
  rules:
    - http:
        paths:
          - path: /api/auth-service
            pathType: Prefix
            backend:
              service:
                name: service1-service
                port:
                  number: 80


